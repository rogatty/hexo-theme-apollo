extends partial/layout

block site_title
    != page.title + " · " + config.title

block description
    - var description = page.description || page.title + ' - ' + config.author;
    meta(name="description", content=description)

block container
    include mixins/post
    +post(page)

block pagination
    include mixins/paginator
    +post()
    include partial/comment

block copyright
    include partial/copyright